<Window x:Class="AudioCaptureWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Audio Recorder" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ComboBox x:Name="devicesListBox" Grid.Row="0" Margin="10" VerticalAlignment="Center" Width="200" SelectionChanged="DevicesListBox_SelectionChanged"/>
        <TextBlock x:Name="currentNoteTextBlock" Grid.Row="1" Margin="10"/>
    </Grid>
</Window>
